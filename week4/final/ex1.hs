module Ex1 where

-- Question 1: Why does ordering matter if we have tuple (a, b) and a 2 element list [a, b] (page 133). 
-- Questiom 2: Do infinite lists that pass their bounds keep on generating? (page 118)
-- Question 3: example 4.25: what exactly is happening here? (page 134)